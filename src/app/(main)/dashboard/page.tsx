import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

const DashBoardPage = () => {
  return (
    <main>
      <h1>This is the where all the fun happens</h1>
    </main>
  );
};

export default DashBoardPage;
